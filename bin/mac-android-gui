#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
APP="${ROOT}/mac-android.app"

if [[ ! -d "$APP" ]]; then
  echo "App not built yet. Building..."
  "${ROOT}/gui/build.sh"
fi

open "$APP"
